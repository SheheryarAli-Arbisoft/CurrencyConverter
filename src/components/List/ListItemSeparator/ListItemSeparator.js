import React from 'react';
import { StyledListItemSeparator } from './styled';
import { propTypes, defaultProps } from './props';

export const ListItemSeparator = () => {
  return <StyledListItemSeparator />;
};

ListItemSeparator.propTypes = propTypes;
ListItemSeparator.defaultProps = defaultProps;
